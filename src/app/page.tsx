import { UnicodeRenderer } from "@/app/components/UnicodeRenderer";

export default function HomePage() {
	return (
		<main className="flex flex-col items-center justify-center h-screen">
			<UnicodeRenderer />
		</main>
	);
}
